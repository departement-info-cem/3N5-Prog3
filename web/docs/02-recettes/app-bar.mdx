---
hide_table_of_contents: true
---

# Barre d'application

<Row>

    <Column size="6">

        Cette barre d'application permet d'afficher une barre en haut de l'écran.

        Il est possible de :

        - Changer le titre de la barre.
        - Ajouter des options de menu.
        - Ajouter un menu hamburger.

    </Column>

    <Column size="6">

        ![Barre d'application](_app-bar/app_bar.png)

    </Column>

</Row>

## Activer la barre d'application

Par défaut, cette barre n'est pas affichée. 

Pour l'activer, il faut :

1. Naviguer dans `res/values/themes`, et modifier les 2 fichiers nommés `themes.xml`.
2. Effacer tous les `.NoActionBar`. Par exemple, `Theme.Material3.DayNight.NoActionBar` devient `Theme.Material3.DayNight`

## Changer le titre

Une fois que la barre d'application est activée, il est possible de changer le texte qui s'y affiche.

Dans le `onCreate` de votre activité, faire comme suit :

```kotlin
class AccueilActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        
        // Reste du code...

        title = "Accueil"
    }
}
```

## Ajouter un menu

Référez-vous à la recette sur les [menus](./menu) pour plus de détails.

## Ajouter un menu hamburger

Référez-vous à la recette sur les [tiroirs de navigation](./tiroir) pour plus de détails.
